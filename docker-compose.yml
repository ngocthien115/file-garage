version: '3.8'

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5000:8080"
    volumes:
      - ./server/data:/app/data   # persist SQLite DB
    restart: unless-stopped
